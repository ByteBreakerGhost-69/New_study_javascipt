// Arrow function adalah cara menulis fungsi le ih singkat dengan menggunakkan '=>' selain lenih rapi, dia juga punya pernedaan dengan kata kunci thia.

<! contoh kode:
const mahasiswa = ['Budi', 'Ani', 'Caca'];

// Versi Ringkas
const jumlahHuruf = mahasiswa.map(nama => nama.length);
console.log(jumlahHuruf); // [4, 3, 4]
